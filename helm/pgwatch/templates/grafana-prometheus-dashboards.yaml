{{- if eq .Values.pgwatch.prometheus.enable_prom_sink "true" }}
{{- range $path, $_ := .Files.Glob "dashboards/prometheus/*.json" }}
{{- $filename := base $path }}
{{- $name := $filename | lower | replace "." "-" | replace "_" "-" }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-prometheus-dashboard-{{ $name }}
  {{- if $.Values.pgwatch.grafana.useSubchart }}
  annotations:
    # Used by the k8s-sidecar (pgwatch.grafana.useSubchart: true) to place
    # dashboards under /tmp/dashboards/prometheus/.
    # Harmless when useSubchart: false — no sidecar is running in that mode.
    grafana_dashboard_folder: "prometheus"
  {{- end }}
  labels:
    application: pgwatch
    vendor: opensource.cybertec
    grafana_dashboard: "1"
data:
  {{ $filename }}: |-
{{ $.Files.Get $path | indent 4 }}
{{- end }}
{{- end }}
